{
  "/* debugMode options */": "0 = No debug, 1 = normal debug, 2 = debug without error terminal output (for not cluttering up test outputs)",
  "debugMode": 1,

  "/* about outputStacktrace */": "outputStacktrace only works when debugMode = 1",
  "outputStacktrace": true,
  "/* about enableLogging */": "When debugMode = 2 then logging is always turned off, no matter what this setting says",
  "enableLogging": true,
  "defaultMaxRetries": 2
}
