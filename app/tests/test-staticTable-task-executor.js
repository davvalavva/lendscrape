const { test } = require('tap')
const staticTableTaskExecutor = require('../task-factory/staticTable-task-executor.js')

test('staticTableTaskExecutor({ task, creditor })', (t) => {
  // TODO
  t.end()
})
